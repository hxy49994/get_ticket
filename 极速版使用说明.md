# 极速三步点击版 - 2 秒内完成抢票

## 核心特性

**⚡ 2 秒内完成三步点击：**
```
立即购票 → 确认场次 → 立即提交
```

---

## 时间配置

### 默认参数
| 参数 | 默认值 | 说明 |
|------|--------|------|
| 每步点击次数 | 2 次 | 减少延迟 |
| 点击间隔 | 100ms | 每步内两次点击的间隔 |
| 步间隔 | 600ms | 每步之间的等待时间 |

### 总耗时计算
```
步骤 1: 2 次点击 × 100ms = 200ms
等待：600ms
步骤 2: 2 次点击 × 100ms = 200ms
等待：600ms
步骤 3: 2 次点击 × 100ms = 200ms
─────────────────────────────
总计：2000ms (2 秒)
```

---

## 时间调整建议

### 如果 2 秒太慢（需要更快）
```
每步次数：2 次 → 1 次
步间隔：600ms → 500ms

总耗时：约 1500ms (1.5 秒)
```

### 如果页面加载慢（需要更慢）
```
步间隔：600ms → 700ms

总耗时：约 2300ms (2.3 秒)
```

---

## 执行时序图

```
T+0ms      步骤 1 点击 1
T+100ms    步骤 1 点击 2
T+700ms    等待结束 → 步骤 2
T+700ms    步骤 2 点击 1
T+800ms    步骤 2 点击 2
T+1400ms   等待结束 → 步骤 3
T+1400ms   步骤 3 点击 1
T+1500ms   步骤 3 点击 2
T+2000ms   完成！
```

---

## 坐标配置

| 步骤 | 按钮 | 默认 Y 坐标 | 调整方法 |
|------|------|----------|----------|
| 1 | 立即购票 | 2600 | 偏上→调大，偏下→调小 |
| 2 | 确认场次 | 1800 | 偏上→调大，偏下→调小 |
| 3 | 立即提交 | 2550 | 偏上→调大，偏下→调小 |

**X 坐标固定：630（屏幕中间）**

---

## 使用流程

1. **设置抢票时间**（如 10:00:00）
2. **配置坐标**（默认即可，需要时调整）
3. **点击「开始倒计时」**
4. **打开大麦 App，到达「立即购票」页面**
5. **2 秒内自动完成三步点击**

---

## 故障排查

### 点击太快，页面没反应过来
```
增加步间隔：600ms → 800ms
```

### 点击太慢，抢不到票
```
减少步间隔：600ms → 500ms
每步次数：2 次 → 1 次
```

### 某一步点击没反应
```
1. 检查该步 Y 坐标
2. 增加该步等待时间
3. 检查页面是否正确加载
```

---

## 日志查看

```bash
adb logcat | grep "步骤"
```

日志示例：
```
步骤 1 点击 1/2 @ Y=2600, 耗时 0ms
步骤 1 点击 2/2 @ Y=2600, 耗时 100ms
进入步骤 2: 确认场次，已耗时 700ms
步骤 2 点击 1/2 @ Y=1800, 耗时 700ms
...
极速三步点击完成，总耗时 2000ms
```

---

## 总结

| 配置 | 默认值 | 可调整范围 |
|------|--------|-----------|
| 每步次数 | 2 次 | 1-3 次 |
| 点击间隔 | 100ms | 50-200ms |
| 步间隔 | 600ms | 400-800ms |
| **总耗时** | **2000ms** | **1500-2500ms** |

**⚡ 极速模式：2 秒内完成三步点击！**
