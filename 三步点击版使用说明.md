# 三步点击版 - 使用说明

## 核心功能

**三步连续点击：**
```
步骤 1: 立即购票
  ↓ (等待 500ms)
步骤 2: 确认场次（确定）
  ↓ (等待 500ms)
步骤 3: 立即提交
```

---

## 点击流程详解

| 步骤 | 按钮名称 | 默认 Y 坐标 | 说明 |
|------|----------|----------|------|
| 1 | 立即购票 | 2600 | 票品详情页底部按钮 |
| 2 | 确认场次 | 1800 | 场次选择弹窗的「确定」按钮 |
| 3 | 立即提交 | 2550 | 订单确认页底部按钮 |

**X 坐标固定：630（屏幕水平中间）**

---

## 使用流程

### 1. 设置抢票时间
```
时/分/秒 → 如 10:00:00
```

### 2. 配置三步坐标
```
步骤 1 Y: 2600（立即购票按钮）
步骤 2 Y: 1800（确认场次按钮）
步骤 3 Y: 2550（立即提交按钮）
```

### 3. 设置点击参数
```
间隔：500ms（每步之间的等待时间）
次数：3 次（每步点击 3 次）
```

### 4. 开始倒计时
```
点击「开始倒计时」按钮
```

### 5. 手动打开大麦
```
倒计时开始后，打开大麦 App
到达「立即购票」页面
```

### 6. 自动执行三步点击
```
倒计时到 00:00:00
→ 步骤 1: 点击「立即购票」3 次
→ 等待 500ms
→ 步骤 2: 点击「确认场次」3 次
→ 等待 500ms
→ 步骤 3: 点击「立即提交」3 次
→ 完成！
```

---

## 坐标校准

### 默认配置
```
步骤 1 Y = 2600  → 屏幕底部（立即购票）
步骤 2 Y = 1800  → 屏幕中部（确认场次弹窗）
步骤 3 Y = 2550  → 屏幕底部（立即提交）
```

### 如何调整

**步骤 1 点击偏上** → Y 调大（2600 → 2650）
**步骤 1 点击偏下** → Y 调小（2600 → 2550）

**步骤 2 点击偏上** → Y 调大（1800 → 1850）
**步骤 2 点击偏下** → Y 调小（1800 → 1750）

**步骤 3 点击偏上** → Y 调大（2550 → 2600）
**步骤 3 点击偏下** → Y 调小（2550 → 2500）

---

## 点击参数说明

| 参数 | 默认值 | 作用 | 调整建议 |
|------|--------|------|----------|
| 间隔 | 500ms | 每步之间的等待时间 | 页面加载慢时调大 |
| 次数 | 3 次 | 每步点击次数 | 网络差时调大 |

### 推荐配置

| 场景 | 间隔 | 次数 |
|------|------|------|
| 网络好 | 300ms | 2 次 |
| 网络一般 | 500ms | 3 次 |
| 网络差 | 800ms | 5 次 |

---

## 执行时序图

```
T+0ms     步骤 1 点击 1
T+500ms   步骤 1 点击 2
T+1000ms  步骤 1 点击 3
T+1500ms  等待 → 步骤 2
T+2000ms  步骤 2 点击 1
T+2500ms  步骤 2 点击 2
T+3000ms  步骤 2 点击 3
T+3500ms  等待 → 步骤 3
T+4000ms  步骤 3 点击 1
T+4500ms  步骤 3 点击 2
T+5000ms  步骤 3 点击 3
T+5500ms  完成！
```

**总耗时：约 5-6 秒**

---

## 注意事项

### 1. 页面必须正确
倒计时结束时，必须停留在「立即购票」页面，否则第一步点击会失败。

### 2. 弹窗必须出现
步骤 2 的「确认场次」弹窗必须在屏幕上，否则点击无效。

### 3. 不要手动操作
三步点击过程中，不要手动点击屏幕。

### 4. 关闭通知
关闭系统通知，避免弹窗遮挡按钮。

---

## 故障排查

### 步骤 1 失败（立即购票没反应）
```
1. 检查 Y 坐标（调整 ±50）
2. 确保在正确页面
3. 增加点击次数（3 → 5）
```

### 步骤 2 失败（确认场次没反应）
```
1. 检查弹窗是否出现
2. 调整 Y 坐标（确认弹窗位置）
3. 增加间隔时间（500 → 800）
```

### 步骤 3 失败（立即提交没反应）
```
1. 检查 Y 坐标（调整 ±50）
2. 确保订单信息已加载
3. 检查是否有验证码弹窗
```

---

## 日志查看

```bash
# 连接手机后查看日志
adb logcat | grep "步骤"

# 查看完整点击日志
adb logcat | grep "Tap"
```

---

## 总结

**三步点击版自动完成：**
```
立即购票 → 确认场次 → 立即提交
```

**你只需：**
- 打开大麦 App
- 到达「立即购票」页面
- 开始倒计时
- 等待完成

**总耗时：约 5-6 秒**
