# 简化版抢票助手 - 使用说明

## 核心功能

**一句话：设置时间 → 打开大麦 → 到点自动点击**

---

## 一、安装步骤

### 1. 在 Aircode 中打开项目
```
File → Open → 选择 D:\get_ticket 文件夹
```

### 2. 连接手机
- USB 连接华为 Mate60 Pro
- 手机确认"允许 USB 调试"

### 3. 运行安装
- 点击右上角绿色运行按钮 ▶️
- 等待安装完成

---

## 二、使用流程

### 第一步：设置抢票时间
```
打开 App → 设置时/分/秒
例如：10:00:00
```

### 第二步：设置点击坐标
```
X 坐标：630  (屏幕水平中间)
Y 坐标：2600 (屏幕底部，立即购买按钮位置)

提示：先手动打开大麦，找到按钮位置后调整坐标
```

### 第三步：设置点击参数
```
点击间隔：50ms (越快越好)
点击次数：3 次 (防止网络延迟导致失败)
```

### 第四步：开始倒计时
```
点击「开始倒计时」按钮
→ 关闭 App，切换到大麦
→ 等待倒计时结束
```

### 第五步：自动点击
```
倒计时到 00:00:00
→ 程序自动点击指定坐标
→ 连续点击 3 次
→ 完成抢票
```

---

## 三、坐标校准方法

### 方法 1：估算
```
华为 Mate60 Pro 分辨率：1260 × 2720

坐标参考：
- 屏幕水平中间：X = 630
- 屏幕底部：Y = 2500 ~ 2700
- 屏幕中部：Y = 1300 ~ 1500
```

### 方法 2：截图查看
```bash
# 在电脑上用 ADB 截图
adb shell screencap -p /sdcard/screen.png
adb pull /sdcard/screen.png

# 用图片查看器打开，查看按钮坐标
```

### 方法 3：调试日志
```
在 App 中添加日志输出当前触摸位置
```

---

## 四、常见问题

### Q1: 点击没反应
**A:** 
1. 检查坐标是否正确（按钮可能在其他位置）
2. 确保大麦 App 在前台
3. 可能需要开启无障碍权限

### Q2: 倒计时不准确
**A:**
- 系统定时器有误差，建议提前 1-2 秒设置时间

### Q3: 需要手动打开大麦吗？
**A:**
- 是的，简化版不会自动打开 App
- 你需要自己提前打开大麦，停留在抢票页面

### Q4: 能自动跳转到支付宝吗？
**A:**
- 简化版只做点击操作
- 提交订单后需要手动确认支付

---

## 五、进阶配置

### 修改默认坐标
编辑 `EntryPage_Simple.ets`：
```typescript
@State clickX: number = 630;    // 修改 X 坐标
@State clickY: number = 2600;   // 修改 Y 坐标
```

### 修改点击速度
```typescript
@State clickInterval: number = 30;  // 30ms 间隔（更快）
```

### 增加点击次数
```typescript
@State clickCount: number = 5;  // 点击 5 次
```

---

## 六、权限说明

| 权限 | 用途 | 是否必须 |
|------|------|----------|
| 悬浮窗 | 显示倒计时 | 推荐 |
| 无障碍 | 模拟点击 | 必须 |

**开启无障碍权限：**
```
设置 → 安全 → 无障碍 → 应用列表 → 抢票助手 → 开启
```

---

## 七、使用建议

1. **提前测试**：非抢票时间先测试点击是否准确
2. **保持前台**：抢票时确保 App 在前台运行
3. **关闭省电**：设置 → 电池 → 关闭省电模式
4. **网络稳定**：使用 WiFi 或 5G 网络

---

## 八、完整代码文件

简化版核心文件：
```
entry/src/main/ets/pages/EntryPage_Simple.ets  ← 主页面
entry/src/main/ets/services/ClickService.ets   ← 点击服务
entry/src/main/ets/extensions/AccessExtension.ets  ← 无障碍服务
```

---

## 九、技术支持

遇到问题查看日志：
```bash
adb logcat | grep -E "Ticket|Click"
```
