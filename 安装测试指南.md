# 安装到华为 Mate60 Pro - 完整步骤

## 一、准备工作

### 1. 在 Aircode 中打开项目

```
步骤：
1. 打开 Aircode IDE
2. File → Open
3. 选择 D:\get_ticket 文件夹
4. 点击 OK
5. 等待 Hvigor 依赖同步完成（首次可能需要几分钟）
```

### 2. 确认项目结构

```
get_ticket/
├── entry/src/main/ets/pages/
│   └── EntryPage_UltraFast.ets  ← 这是主页面
├── entry/src/main/module.json5  ← 模块配置
└── ...
```

---

## 二、修改入口页面（重要！）

当前项目有多个页面文件，需要指定使用哪个作为入口：

### 修改 module.json5

```
entry/src/main/module.json5
```

找到 `abilities` 部分，修改 `mainElement` 对应的页面：

```json5
{
  "abilities": [
    {
      "name": "EntryAbility",
      "srcEntry": "./ets/entryability/EntryAbility.ets",
      // ... 其他配置
    }
  ]
}
```

### 修改 EntryAbility.ets

```
entry/src/main/ets/entryability/EntryAbility.ets
```

修改 `onWindowStageCreate` 中的页面加载：

```typescript
onWindowStageCreate(windowStage: window.WindowStage): void {
  // 修改这里，加载超极速版页面
  windowStage.loadContent('pages/EntryPage_UltraFast', (err, data) => {
    // ...
  });
}
```

---

## 三、配置签名（必须）

### 方法 1：自动签名（推荐）

```
1. File → Project Structure
2. 选择 Signing Configs
3. 勾选 "Automatically generate signature"
4. 登录华为账号
5. 点击 Apply
```

### 方法 2：手动签名

如果自动签名失败：
```
1. 访问 https://developer.harmonyos.com/cn/docs
2. 下载证书工具
3. 生成 .p7b 证书文件
4. 在 Signing Configs 中导入证书
```

---

## 四、连接手机

### 1. USB 连接

```
1. 用 USB 数据线连接华为 Mate60 Pro 和电脑
2. 手机上弹出提示时，选择 "传输文件" 模式
3. 确认 USB 调试已开启
```

### 2. 开启 USB 调试

```
设置 → 关于手机 → 连续点击"版本号" 7 次
设置 → 系统和更新 → 开发人员选项
  ✓ USB 调试
  ✓ 仅充电模式下允许 ADB 调试
```

### 3. 确认设备连接

```
在 Aircode 右上角查看设备列表
应该看到你的 Mate60 Pro 设备型号
如果没有，点击刷新按钮
```

---

## 五、安装运行

### 方式 1：点击运行按钮

```
1. 确保设备已连接
2. 点击右上角绿色运行按钮 ▶️
3. 或按 Shift + F10
4. 等待编译安装（首次编译约 2-3 分钟）
5. 安装完成后自动启动 App
```

### 方式 2：命令行安装

```bash
# 在终端执行
hvigorw assembleDebug
hvigorw install

# 或使用 ADB
adb install entry/build/outputs/default/debug/entry-default-unsigned.hap
```

---

## 六、首次启动权限

### 1. 允许未知来源

如果提示"禁止安装"：
```
设置 → 安全 → 更多安全设置
  → 开启 "安装外部来源应用"
```

### 2. 授予权限

启动 App 后：
```
1. 允许悬浮窗权限
2. 允许通知权限（可选）
```

---

## 七、测试步骤

### 1. 打开 App

```
手机桌面 → 找到 "超极速三步抢票" 图标 → 打开
```

### 2. 设置测试时间

```
建议设置一个 1 分钟后的时间进行测试
例如：当前 10:00，设置 10:01:00
```

### 3. 开始倒计时

```
点击 "开始倒计时" 按钮
```

### 4. 打开大麦 App

```
倒计时开始后，手动打开大麦 App
到达 "立即购票" 页面
```

### 5. 观察日志

```bash
# 在电脑上查看日志
adb logcat | grep -E "步骤|Tap|极速"
```

---

## 八、常见问题

### Q1: 设备列表为空
```
解决：
1. 检查 USB 线是否正常
2. 重新插拔 USB
3. 确认 USB 调试已开启
4. 尝试更换 USB 端口
```

### Q2: 编译失败
```
解决：
1. File → Invalidate Caches → Restart
2. 删除 .hvigor 文件夹，重新同步
3. 检查 SDK 是否完整
```

### Q3: 安装失败
```
解决：
1. 卸载旧版本
2. 检查签名配置
3. 清理构建：hvigorw clean
```

### Q4: App 闪退
```
解决：
1. 查看 logcat 日志
2. 检查权限是否授予
3. 确认 module.json5 配置正确
```

---

## 九、调试技巧

### 查看实时日志

```bash
# 连接手机后执行
adb logcat -c  # 清空日志
adb logcat | grep Ticket
```

### 热重载（修改代码后无需重新安装）

```
修改代码后按 Ctrl + F9
App 会自动刷新
```

### 截图校准坐标

```bash
# 截图
adb shell screencap -p /sdcard/screen.png

# 拉取到电脑
adb pull /sdcard/screen.png screen.png
```

---

## 十、测试清单

```
□ 项目能在 Aircode 中打开
□ 依赖同步完成无报错
□ 签名配置成功
□ 手机连接成功
□ 编译无错误
□ App 能正常安装
□ App 能正常启动
□ 倒计时功能正常
□ 点击坐标正确
□ 三步点击在 1 秒内完成
```

---

## 快速开始

```
1. Aircode 打开 D:\get_ticket
2. 修改 EntryAbility.ets 加载 EntryPage_UltraFast
3. 配置签名（自动签名）
4. USB 连接手机
5. 点击运行按钮 ▶️
6. 测试！
```

---

## 联系支持

遇到问题时查看日志：
```bash
adb logcat | grep -E "Ticket|Tap|步骤"
```
