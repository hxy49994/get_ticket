# 鸿蒙抢票助手 - 详细操作指南

## 一、开发环境准备

### 1. 安装 Aircode / DevEco Studio

**Aircode（推荐）：**
- 下载地址：https://aircode.io
- 支持鸿蒙原生开发一站式 IDE

**DevEco Studio（官方）：**
- 下载地址：https://developer.harmonyos.com/cn/develop/deveco-studio
- 需要华为账号登录

### 2. 配置鸿蒙 SDK

首次打开 IDE 会自动下载 SDK，如果没有：
1. `File` → `Settings` → `Languages & Frameworks` → `HarmonyOS`
2. 点击 `SDK Manager` 下载所需 SDK
3. 选择 **HarmonyOS 4.0+** (API 10)

### 3. 手机连接准备

**在华为 Mate60 Pro 上：**
```
设置 → 关于手机 → 连续点击"版本号" 7 次 → 开启开发者模式

设置 → 系统和更新 → 开发人员选项：
  ✓ USB 调试
  ✓ 仅充电模式下允许 ADB 调试（建议开启）
  ✗ USB 调试（安全模式）← 不要开启
```

---

## 二、导入项目

### 方式一：直接打开文件夹

1. 打开 Aircode / DevEco Studio
2. `File` → `Open` → 选择 `D:\get_ticket` 文件夹
3. 点击 `OK`
4. 等待 Hvigor 依赖同步完成（首次可能需要几分钟）

### 方式二：创建新项目后替换

1. `File` → `New` → `New Project`
2. 选择 `Empty Ability` 模板
3. 项目名称：`DamaiTicket`
4. 选择 SDK 版本：`HarmonyOS 4.0+`
5. 创建完成后，将 `D:\get_ticket/entry` 目录下的文件覆盖到新建项目的 `entry` 目录

---

## 三、项目结构说明

```
get_ticket/
├── entry/                          # 主模块
│   ├── src/main/
│   │   ├── ets/
│   │   │   ├── pages/
│   │   │   │   ├── EntryPage.ets           # 主页面
│   │   │   │   └── FloatWindowAbility.ets  # 悬浮窗
│   │   │   ├── entryability/
│   │   │   │   └── EntryAbility.ets        # 应用入口
│   │   │   ├── services/
│   │   │   │   └── TicketServiceAbility.ets # 抢票服务
│   │   │   └── extensions/
│   │   │       └── TicketWorkExtension.ets  # 定时任务
│   │   ├── resources/
│   │   │   └── base/element/
│   │   │       ├── string.json             # 字符串
│   │   │       └── color.json              # 颜色
│   │   └── module.json5                    # 模块配置
│   └── build-profile.json5
├── build-profile.json5              # 全局构建配置
└── hvigorfile.ts                    # 构建脚本
```

---

## 四、签名配置（必须）

鸿蒙应用需要签名才能安装到真机。

### 1. 自动签名（推荐）

1. 打开 `File` → `Project Structure` → `Signing Configs`
2. 选择 `Automatically generate signature`
3. 登录华为账号
4. 点击 `Apply`

### 2. 手动签名

如果没有自动签名成功：
1. 下载证书：https://developer.harmonyos.com/cn/docs/documentation/doc-guides-V3/ide-auto-sign-certificate-0000001506751367
2. 在 `Signing Configs` 中导入 `.p7b` 证书文件

---

## 五、运行到手机

### 步骤

1. **连接手机**
   - USB 连接 Mate60 Pro
   - 手机弹出"允许 USB 调试"时点击"允许"

2. **选择设备**
   - IDE 右上角会显示设备列表
   - 选择你的 Mate60 Pro（显示设备型号）

3. **点击运行**
   - 点击绿色运行按钮 ▶️ 或 `Shift + F10`
   - IDE 会自动编译并安装 APK 到手机

4. **首次安装权限**
   - 如果提示"禁止安装"，去设置里允许"未知来源应用"

---

## 六、使用说明

### 1. 打开应用

安装完成后，在手机桌面找到「抢票助手」图标并打开

### 2. 设置抢票时间

```
主界面 → 点击 时/分/秒 选择器
→ 设置目标时间（如 10:00:00）
→ 时间自动保存
```

### 3. 开启悬浮窗权限

首次使用前必须开启：

```
设置 → 应用和服务 → 应用管理 → 抢票助手 → 权限
→ 悬浮窗 → 选择"允许"
```

或在应用内点击「显示悬浮窗」按钮，系统会跳转授权

### 4. 开始倒计时

```
点击「开始倒计时」按钮
→ 开始显示倒计时
→ 悬浮窗会在后台显示剩余时间
```

### 5. 抢票执行

到达设定时间后：
1. 应用自动打开大麦 App
2. 导航到票品页面
3. 点击购买按钮
4. 提交订单
5. 跳转支付宝

---

## 七、关键权限说明

| 权限 | 用途 | 是否必须 |
|------|------|----------|
| `INTERNET` | 访问网络 | ✓ |
| `SYSTEM_ALERT_WINDOW` | 悬浮窗显示 | ✓ |
| `ACCESS_BACKGROUND_LOCATION` | 后台任务调度 | 推荐 |

**注意：** 鸿蒙 6.1 可能限制某些自动化操作，如果自动点击不生效，需要：
1. 设置 → 应用 → 抢票助手 → 电池 → 允许后台活动
2. 设置 → 安全 → 无障碍 → 抢票助手 → 开启「无障碍服务」（如果支持）

---

## 八、常见问题

### Q1: 编译报错 "SDK not found"
**A:** 在 SDK Manager 中下载对应的 HarmonyOS SDK

### Q2: 设备列表为空
**A:** 
- 检查 USB 线是否连接正常
- 确认手机已开启 USB 调试
- 尝试更换 USB 端口

### Q3: 安装失败 "Application not installed"
**A:** 
- 清除旧版本数据后重试
- 检查签名配置是否正确

### Q4: 悬浮窗不显示
**A:** 
- 检查是否已授予悬浮窗权限
- 重启应用

### Q5: 抢票流程不自动执行
**A:** 
- 鸿蒙 6.1 限制后台自动点击
- 可能需要手动确认每个步骤
- 考虑申请无障碍服务权限

---

## 九、调试技巧

### 1. 查看日志

```bash
# 通过命令行查看日志
adb logcat | grep TicketApp

# 或在 IDE 中打开 Logcat 面板
```

### 2. 断点调试

```typescript
// 在代码中添加日志
console.info('抢票开始:', JSON.stringify(data));

// 使用断点
if (condition) {
  debugger; // 在此行设置断点
}
```

### 3. 热重载

修改代码后按 `Ctrl + F9` 可热重载，无需重新安装

---

## 十、打包发布

### 生成 HAP 包

```bash
# Debug 版本
hvigorw assembleDebug

# Release 版本
hvigorw assembleRelease
```

输出位置：
- Debug: `entry/build/outputs/default/debug/entry-default-unsigned.hap`
- Release: `entry/build/outputs/default/release/entry-release-signed.hap`

### 安装到手机

```bash
# 通过 ADB 安装
adb install entry/build/outputs/default/debug/entry-default-unsigned.hap
```

---

## 十一、优化建议

1. **添加错误重试机制**：网络失败时自动重试
2. **支持多场次**：可添加多个抢票任务
3. **通知提醒**：抢票成功/失败时推送通知
4. **日志导出**：方便排查问题

---

## 十二、联系方式

遇到问题可查看：
- 官方文档：https://developer.harmonyos.com/cn/docs
- 鸿蒙开发者社区：https://developer.harmonyos.com/cn/forums
