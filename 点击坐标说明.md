# 抢票流程 - 点击坐标详细说明

## 华为 Mate60 Pro 屏幕规格
```
分辨率：1260 × 2720 (像素)
屏幕尺寸：6.82 英寸
```

---

## 完整抢票流程（7 个步骤）

### 步骤 1：打开大麦 App
```
操作：启动应用
坐标：不需要点击（自动启动）
时间：提前 30 秒
```

### 步骤 2：进入「我的」页面
```
操作：点击底部导航栏「我的」
坐标：X=630, Y=2650
说明：底部导航栏最右侧按钮
```

### 步骤 3：点击「我的门票」
```
操作：在「我的」页面点击「我的门票」
坐标：X=630, Y=1500
说明：根据实际 UI 可能需要调整
```

### 步骤 4：选择待抢场次
```
操作：点击目标场次卡片
坐标：X=630, Y=800（第一个场次）
说明：如果有多个场次，Y 坐标递增
```

### 步骤 5：点击「立即购买」
```
操作：点击底部购买按钮
坐标：X=630, Y=2600
说明：屏幕底部固定位置的按钮
```

### 步骤 6：选择价格档位
```
操作：点击价格选项
坐标：X=630, Y=1200（第一个价格档）
说明：不同价格档位 Y 坐标不同
```

### 步骤 7：提交订单
```
操作：点击「提交订单」按钮
坐标：X=630, Y=2550
说明：订单确认页面底部按钮
```

### 步骤 8：跳转支付宝
```
操作：自动跳转或点击支付
坐标：X=630, Y=2400
说明：支付宝支付确认按钮
```

---

## 坐标参考表

| 步骤 | 按钮名称 | X 坐标 | Y 坐标 | 备注 |
|------|----------|-------|-------|------|
| 1 | 我的（底部导航） | 630 | 2650 | 底部导航栏 |
| 2 | 我的门票 | 630 | 1500 | 列表项 |
| 3 | 场次选择 | 630 | 800 | 场次卡片 |
| 4 | 立即购买 | 630 | 2600 | 底部按钮 |
| 5 | 价格档位 | 630 | 1200 | 价格列表 |
| 6 | 提交订单 | 630 | 2550 | 底部按钮 |
| 7 | 立即支付 | 630 | 2400 | 支付宝页面 |

---

## 简化版配置（单点点击）

当前简化版只支持**单个坐标点击**，适用于最关键的步骤：

### 场景 A：已经在抢票页面，只点击「立即购买」
```
X = 630
Y = 2600
```

### 场景 B：在订单确认页，点击「提交订单」
```
X = 630
Y = 2550
```

### 场景 C：支付宝页面，点击「立即支付」
```
X = 630
Y = 2400
```

---

## 完整流程版（多步骤自动化）

如果需要完整流程自动化，需要修改代码支持**多步骤序列**：

```typescript
// 抢票步骤序列
const steps = [
  { delay: 1000, x: 630, y: 2650,  desc: '点击我的' },
  { delay: 2000, x: 630, y: 1500,  desc: '点击我的门票' },
  { delay: 1000, x: 630, y: 800,   desc: '选择场次' },
  { delay: 3000, x: 630, y: 2600,  desc: '点击立即购买' },
  { delay: 1000, x: 630, y: 1200,  desc: '选择价格' },
  { delay: 1000, x: 630, y: 2550,  desc: '提交订单' },
  { delay: 2000, x: 630, y: 2400,  desc: '支付宝支付' }
];

// 执行序列
async function executeSteps() {
  for (const step of steps) {
    await sleep(step.delay);
    tap(step.x, step.y);
  }
}
```

---

## 坐标校准方法

### 方法 1：手动估算
```
屏幕高度 2720 像素，分为 10 等份：
- 顶部 10%：Y = 0 ~ 272
- 上部 20%：Y = 272 ~ 544
- 中上部 30%：Y = 544 ~ 816
- 中部 40%：Y = 816 ~ 1088
- 中下部 50%：Y = 1088 ~ 1360
- 下部 60%：Y = 1360 ~ 1632
- 底部 70%：Y = 1632 ~ 1904
- 底部 80%：Y = 1904 ~ 2176
- 底部 90%：Y = 2176 ~ 2448
- 底部 100%：Y = 2448 ~ 2720
```

### 方法 2：截图分析
```bash
# 1. 截图
adb shell screencap -p /sdcard/screen.png

# 2. 拉取到电脑
adb pull /sdcard/screen.png

# 3. 用图片编辑器打开，查看像素坐标
```

### 方法 3：日志输出触摸位置
```typescript
// 在 App 中添加触摸监听
onTouch() {
  console.info(`触摸位置：X=${event.x}, Y=${event.y}`);
}
```

---

## 注意事项

### 1. 大麦 UI 可能变化
每个版本的大麦 App UI 可能不同，坐标需要重新校准。

### 2. 弹窗干扰
可能有以下弹窗需要处理：
- 登录弹窗
- 活动弹窗
- 验证码

### 3. 网络延迟
点击后需要等待页面加载，建议增加 `delay` 时间。

### 4. 屏幕旋转
确保屏幕锁定在竖屏模式，避免坐标偏移。

---

## 推荐配置

### 最简配置（只点击购买按钮）
```
X = 630
Y = 2600
点击次数 = 3
点击间隔 = 50ms
```

### 完整配置（多步骤）
需要修改代码支持步骤序列，参考上方「完整流程版」。

---

## 下一步优化

1. **图像识别**：用 OpenCV 识别按钮位置，自动适配 UI 变化
2. **OCR 识别**：识别按钮文字，精确定位
3. **UI 树分析**：通过无障碍服务获取 UI 结构，定位控件
4. **自适应**：记录成功点击位置，下次自动使用
