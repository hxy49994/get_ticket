# 极简版 - 使用说明

## 核心功能

**只做一个动作：倒计时结束 → 自动点击「立即购买」按钮**

---

## 使用流程

### 1. 安装 App
```
Aircode → 打开项目 → 点击运行 → 安装到手机
```

### 2. 设置抢票时间
```
设置时/分/秒（如 10:00:00）
```

### 3. 设置点击坐标
```
Y 坐标：2600（默认，屏幕底部）
如果按钮不在正确位置，需要调整 Y 值
```

### 4. 设置点击参数
```
间隔：30ms（超快，可改为 50ms 或 100ms）
次数：5 次（防止网络延迟，可改为 3-10 次）
```

### 5. 开始倒计时
```
点击「开始倒计时」按钮
```

### 6. 手动打开大麦
```
倒计时开始后，手动打开大麦 App
进入「立即购买」页面
```

### 7. 自动点击
```
倒计时到 00:00:00
→ 自动点击 5 次，每次间隔 30ms
→ 总共用时 150ms，极快
```

---

## 坐标校准

### 默认坐标
```
X = 630（屏幕水平中间，固定）
Y = 2600（屏幕底部，可调整）
```

### 如何调整 Y 坐标

**情况 1：点击位置太靠上**（点在按钮上方）
```
Y 值调大：2600 → 2650
```

**情况 2：点击位置太靠下**（点在按钮下方）
```
Y 值调小：2600 → 2550
```

### 校准方法

```
1. 打开大麦 App，停留在「立即购买」页面
2. 用手指点击「立即购买」按钮
3. 观察 App 日志（或修改代码输出坐标）
4. 记录准确点击位置的 Y 值
5. 更新到 App 设置中
```

---

## 点击参数说明

| 参数 | 默认值 | 说明 | 建议 |
|------|--------|------|------|
| 间隔 | 30ms | 两次点击之间的时间 | 30-100ms |
| 次数 | 5 次 | 连续点击次数 | 3-10 次 |

**推荐配置：**
- 网络好：间隔 30ms，次数 5 次
- 网络一般：间隔 50ms，次数 7 次
- 网络差：间隔 100ms，次数 10 次

---

## 注意事项

### 1. 大麦 App 必须在前台
倒计时结束时，大麦 App 必须在屏幕前台，否则点击无效。

### 2. 页面必须正确
确保停留在「立即购买」按钮页面，不是其他页面。

### 3. 不要切换应用
倒计时开始后，不要切换到其他应用。

### 4. 关闭通知弹窗
关闭系统通知，避免弹窗遮挡按钮。

---

## 故障排查

### 点击没反应
```
1. 检查 Y 坐标是否正确（点击位置偏上/偏下）
2. 检查大麦 App 是否在前台
3. 增加点击次数（如 5 次 → 10 次）
4. 增加点击间隔（如 30ms → 50ms）
```

### 倒计时不准
```
建议提前 1-2 秒设置时间
例如：实际 10:00:00 抢票，设置 9:59:58
```

### 点击太慢
```
减少间隔：50ms → 30ms
减少次数：7 次 → 5 次
```

---

## 日志查看

```bash
# 连接手机后查看日志
adb logcat | grep "Tap"

# 或完整日志
adb logcat | grep "Ticket"
```

---

## 总结

**极简版只做一件事：**
```
倒计时结束 → 快速点击指定坐标 → 完成
```

**你负责：**
- 打开大麦 App
- 进入抢票页面
- 确保「立即购买」按钮可见

**App 负责：**
- 倒计时
- 到点自动点击
